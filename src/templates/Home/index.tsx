import Base from '../Base'
import * as S from './styles'

export type HomeTemplateProps = {
}

const Home = ({}: HomeTemplateProps) => (
  <Base>
    xunda
    <S.Button>Test</S.Button>
  </Base>
)

export default Home
